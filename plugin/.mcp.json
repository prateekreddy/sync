{
  "mcpServers": {
    "sync": {
      "type": "http",
      "url": "${SYNC_MCP_URL:-https://mcp.plane.shipitnow.dev/mcp}",
      "headers": {
        "X-Sync-Session": "${CLAUDE_CODE_SESSION_ID:-}"
      }
    }
  }
}
