node_modules/
dist/
*.tsbuildinfo

# Secrets — never commit
.env
deploy/gateway.env

# Plane's self-host artefacts: generated by its own setup.sh, and vendoring a
# third-party AGPL script would pin a version the README tells you to fetch.
deploy/plane/setup.sh
deploy/plane/plane-app/

# Local npm tuning (sandbox network workarounds) — not project config
.npmrc
